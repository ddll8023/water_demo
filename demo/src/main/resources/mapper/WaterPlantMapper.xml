<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.WaterPlantMapper">

    <!-- 分页查询水厂列表（基础信息）- 用于PageHelper分页 -->
    <select id="selectWaterPlantPageByKeyword" resultType="com.example.demo.pojo.entity.facility.WaterPlant">
        SELECT *
        FROM water_plants
        WHERE deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 根据ID查询水厂基础信息 -->
    <select id="selectById" resultType="com.example.demo.pojo.entity.facility.WaterPlant">
        SELECT * FROM water_plants WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <!-- 检查水厂编码是否存在 -->
    <select id="existsByPlantCode" resultType="boolean">
        SELECT COUNT(1) FROM water_plants WHERE plant_code = #{plantCode} AND deleted_at IS NULL
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <!-- 检查水厂名称是否存在 -->
    <select id="existsByName" resultType="boolean">
        SELECT COUNT(1) FROM water_plants WHERE name = #{name} AND deleted_at IS NULL
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <!-- 获取所有可用水厂（用于下拉选择） -->
    <select id="selectAvailableWaterPlants" resultType="com.example.demo.pojo.entity.facility.WaterPlant">
        SELECT id, name, plant_code FROM water_plants
        WHERE deleted_at IS NULL
        ORDER BY name ASC
    </select>

    <!-- 统计水厂总数 -->
    <select id="countTotal" resultType="long">
        SELECT COUNT(1) FROM water_plants WHERE deleted_at IS NULL
    </select>

    <!-- 根据部门ID统计水厂数量 -->
    <select id="countByDepartmentId" resultType="long">
        SELECT COUNT(1) FROM water_plants WHERE department_id = #{departmentId} AND deleted_at IS NULL
    </select>

    <!-- 根据负责人ID统计水厂数量 -->
    <select id="countByManagerId" resultType="long">
        SELECT COUNT(1) FROM water_plants WHERE manager_id = #{managerId} AND deleted_at IS NULL
    </select>

    <!-- 根据管理单位统计水厂数量 -->
    <select id="countByManagementUnit" resultType="long">
        SELECT COUNT(1) FROM water_plants WHERE management_unit = #{managementUnit} AND deleted_at IS NULL
    </select>

    <!-- 查询部门名称 -->
    <select id="selectDepartmentNameByWaterPlantId" resultType="java.lang.String">
        SELECT d.name
        FROM water_plants wp
        LEFT JOIN departments d ON wp.department_id = d.id
        WHERE wp.id = #{waterPlantId} AND d.deleted_at IS NULL
    </select>

    <!-- 查询负责人姓名 -->
    <select id="selectManagerNameByWaterPlantId" resultType="java.lang.String">
        SELECT p.name
        FROM water_plants wp
        LEFT JOIN personnel p ON wp.manager_id = p.id
        WHERE wp.id = #{waterPlantId} AND p.deleted_at IS NULL
    </select>

    <!-- 查询负责人电话 -->
    <select id="selectManagerPhoneByWaterPlantId" resultType="java.lang.String">
        SELECT p.phone
        FROM water_plants wp
        LEFT JOIN personnel p ON wp.manager_id = p.id
        WHERE wp.id = #{waterPlantId} AND p.deleted_at IS NULL
    </select>

</mapper>
